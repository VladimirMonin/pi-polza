# Secrets — never commit
.env
.env.*
!.env.example

# Raw diagnostics that may contain account data or key-adjacent payloads
artifacts/raw/
artifacts/*.raw.json
notes/*.raw.md

# Local metadata cache (OpenRouter snapshot) — machine-local, not source
.pi/pi-polza-cache/

# Local background-task artifacts from the Pi harness — runtime state, not source
.pi/tasks/

# Dependencies / build
node_modules/
dist/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Local package artifacts (npm pack output)
*.tgz
